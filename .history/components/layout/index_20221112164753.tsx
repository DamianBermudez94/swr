import {useMe} from "lib/hooks"

export function layout({children}){
    cons
    return (
    <div>
        <header></header>
        <div>{children}</div>
        <footer></footer>
    </div>
    );
  }